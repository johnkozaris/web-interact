## What

Brief description of the change.

## Why

What problem does this solve?

## Testing

- [ ] `pnpm run verify` passes (typecheck + bundle + test + build)
- [ ] Tested manually with `web-interact --headless open "https://example.com"`
